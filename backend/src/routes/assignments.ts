import { Router, type Request, type Response } from "express";

const assignmentRouter: Router = Router();

assignmentRouter.post("/", (req: Request, res: Response) => {
    
});

assignmentRouter.get("/", (req: Request, res: Response) => {

});

assignmentRouter.get("/:id", (req: Request, res: Response) => {

});

export default assignmentRouter